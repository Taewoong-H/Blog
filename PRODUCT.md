# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**1순위 — 검색으로 온 문제 해결자.** 구글·네이버에서 구체적인 주제("MCP 위젯 아키텍처", "하와이 7박 8일 일정", "LangChain 써야 하나")를 찾다가 글 상세로 **직접 들어오는** 한국어 독자. 홈을 거치지 않는다. 이 사람의 성공은 찾던 답을 이 글에서 얻고 끝까지 읽는 것이다.

**2순위 — 나를 평가하는 사람.** 채용 담당자·동료 개발자가 이 사람이 어떻게 생각하고 일하는지 보려고 훑는다. About 페이지와 아카이브가 실제로 이 목적을 수행하고 있다. 다만 1순위와 동급은 아니다 — 충돌하면 검색 독자의 읽기 경험이 이긴다.

**운영자 — 황태웅 본인.** 에디터에서 `.mdx` 파일을 쓰고 push하는 유일한 작성자. 글쓰기 비용이 올라가면 블로그가 멈춘다.

## Product Purpose

개발 · 일상 · 여행 · 경제를 기록하고, 매 거래일의 주가관찰 결과를 쌓는 개인 블로그(taewoong.log). 글 하나가 파일 하나(`.mdx`)이고, DB·CMS·관리자 페이지·로그인이 없다.

명시된 운영 철학은 **"완벽한 블로그보다 운영 중인 블로그"** — 기능을 늘리는 것보다 공개하고 꾸준히 쌓는 것을 우선한다.

성공은 두 가지다: 검색으로 들어온 사람이 답을 얻고 끝까지 읽는 것, 그리고 아카이브가 멈추지 않고 계속 쌓이는 것.

## Positioning

**독자 기준:** 튜토리얼을 다시 쓴 글이 아니라, 실제 프로덕션 맥락에서 직접 부딪힌 1차 기록이다. 사내 AI 어시스턴트에 MCP를 붙이며 겪은 구조 결정, 실제로 다녀온 7박 8일 일정과 비용 — 옆 블로그가 사실대로 복제할 수 없는 것은 "겪은 사람이 썼다"는 점이다. 결론만이 아니라 **틀린 선택과 그 이유가 함께 남는다**("삽질 끝에 정착하기까지", "선택의 이유를 기록합니다").

**운영 기준:** 콘텐츠 전체가 git에 있는 파일이라 운영 비용이 0에 수렴하고, 발행이 push 한 번이며, 모든 히스토리가 버전 관리된다.

## Operating Context

**글 쓰기 → 발행:** 에디터에서 `content/posts/<category>/<파일>.mdx` 작성 → frontmatter로 메타데이터 지정 → 작성 중에는 `published: false` → `main`에 push → Vercel 자동 빌드·배포. 주가관찰은 외부 배치가 `content/posts/market/`에 MDX와 JSON 한 쌍을 커밋한다.

**이미지:** 원본(JPG/PNG)은 커밋하지 않는다. `npm run images`로 WebP 변환(긴 변 1600px 이하, 용량 목표 기반 품질 자동 조정, 파일명 소문자·하이픈 정규화) 후 **WebP만** 커밋.

**커버 이미지:** frontmatter `cover.variant`(1~4)가 있으면 그 템플릿, 없으면 slug 해시로 결정적 자동 배정. `cover.image`가 있으면 모든 것을 무시하고 그 이미지를 쓴다. 같은 1200×630 산출물이 **카드 그리드 썸네일과 OG 공유 이미지 양쪽에 동일하게** 쓰인다.

**인프라:** 호스팅 Vercel · 도메인/DNS Cloudflare(Vercel 레코드를 DNS only, 프록시 off) · 분석 Vercel Analytics.

**읽는 환경:** 한국어. 검색 결과에서 온 모바일·데스크톱 브라우저. 진입점은 대개 홈이 아니라 글 상세다.

## Capabilities and Constraints

**라우트:** `/` · `/posts` (`?category=<slug>` 필터) · `/posts/market` (주가관찰 목록) · `/posts/<category>/<slug>` · `/about` · `/rss.xml` · `/sitemap.xml` · `/robots.txt` · `/og/<...>` · `/manifest.webmanifest`

**카테고리는 5개로 확정이며 1급 구조다:** 개발(`development`) · 일상(`life`) · 여행(`travel`) · 경제(`economy`) · 주가관찰(`market`). `lib/categories.ts`가 유일한 권위다. 주가관찰은 자동 생성 기록이라 홈 피드에서는 제외하고 전용 조밀한 아카이브에서 노출한다. README와 `lib/site.ts`의 `description`에 남아 있는 "개발, 여행, 투자, 운동"은 **낡은 문구이며 정리 대상** — 새 작업이 이 문구를 진실로 취급하면 안 된다.

**기술 제약:** Next.js 16 App Router · React 19 · Tailwind CSS v4(설정 파일 없이 `@import "tailwindcss"`) · MDX는 `next-mdx-remote-client/rsc` · frontmatter는 `gray-matter` · `remark-gfm`(표) · `rehype-pretty-code`(코드 하이라이팅) · 이미지 변환은 `sharp`. `lib/posts.ts`가 `fs`를 쓰므로 글을 읽는 라우트는 Node 런타임이어야 한다. Satori는 CSS 변수를 지원하지 않아 커버 토큰은 `lib/cover/tokens.ts`에 상수로 하드코딩되어 있다(`globals.css` 토큰과 값을 일치시켜야 함).

**DB·CMS·관리자 페이지·인증은 없고, 앞으로도 발행의 의존성이 되어선 안 된다.**

**과거 금지 사항은 더 이상 구속력이 없다.** `docs/`의 MVP·포트폴리오 프롬프트가 반복해서 금지한 다크모드, 사이트 내 검색, 댓글, 조회수는 **초기 과설계 방지용 MVP 한정 제약이었고 지금은 열려 있다.** 필요하면 도입할 수 있다. 저 문서들을 지속 제약으로 인용하지 말 것.

**미정 (임의로 결정하지 말 것):** 수익화의 구체적 형태.

## Brand Commitments

- **이름:** `taewoong.log` (사이트명) / `Taewoong.dev` (도메인·워드마크, 푸터에서 `.dev`에 액센트 컬러). 저자 황태웅 / "Taewoong".
- **목소리:** 한국어 1인칭. 담백하고 단정적이며, 결론만 요약하지 않고 시행착오와 판단 근거를 남긴다. 마케팅 톤이 아니다.
- **확정 자산:** 프로필 사진(`public/images/profile/img-5331.webp`), 파비콘·PWA 아이콘 세트, Pretendard(본문·제목) + JetBrains Mono(메타데이터·코드·터미널), `lib/cover/`의 4종 커버 템플릿 시스템.
- **부정확한 주장 — 수정 대상:** 푸터의 "매주 화요일 발행하는 개인 기록"은 **약속이 아니라 목표**다. 실제 발행일(6/26, 6/27, 7/1, 7/2, 7/8, 7/23)은 주간 주기가 아니다. 어떤 작업도 이를 보장으로 제시해선 안 되며, 문구는 완화되어야 한다.

## Evidence on Hand

**실재하는 것:**
- 발행된 글 6개 + 미발행 초안 1개(하와이, `published: false`) — `content/posts/`
- 실제 프로필 사진 — `public/images/profile/img-5331.webp`
- 실제 연락처: GitHub `github.com/Taewoong-H`, 이메일 `taeung2008@gmail.com`
- 저자가 직접 적은 이력(About 타임라인): 2021 프론트엔드 개발자로 정착 · 2022 롯데이노베이트 입사 · 2024 AI팀 이동, 플랫폼 서비스 프론트엔드 · 2026 Taewoong.dev 리뉴얼
- 커버/OG 자동 생성 시스템 — `lib/cover/`
- Vercel Analytics 설치됨 (트래픽 데이터는 존재하나 **수치는 확인된 바 없음**)

**없는 것 — 절대 지어내지 말 것:**
방문자·구독자 수치, 트래픽 통계, 추천사·후기, 뉴스레터나 이메일 구독(존재하지 않음), 댓글, 수상·클라이언트 이력, 수익 데이터, LinkedIn 계정(About에 주석 처리되어 있음 = 없음), 현재 진행 중인 제휴 관계.

## Product Principles

1. **모든 화면은 랜딩이다.** 진입점은 홈이 아니라 글 상세다. 검색으로 떨어진 한 사람이 여기가 어디이고 다음에 뭘 읽을지 알 수 있어야 한다.
2. **운영되는 것이 완벽한 것을 이긴다.** 글 쓰는 비용을 올리는 모든 것은 의심 대상이다. 기능 수보다 발행 지속이 우선이다.
3. **시행착오를 지우지 않는다.** 결론만 남기지 않고 틀린 선택과 그 이유를 함께 남긴다. 이것이 이 블로그의 차별점이므로 구조가 이를 밀어내면 안 된다.
4. **5개 카테고리는 태그가 아니라 뼈대다.** 개발·일상·여행·경제·주가관찰은 내비게이션이자 콘텐츠 구조로 유지된다. 단, 자동 생성되는 주가관찰은 직접 쓴 글을 홈에서 밀어내지 않는다.
5. **파일 하나가 글 하나.** 콘텐츠는 git 안의 MDX로 남는다. 런타임 DB나 CMS가 발행의 의존성이 되지 않는다.
